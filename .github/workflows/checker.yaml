name: checker
on:
  push:
    branches:
      - main
  pull_request:
    paths-ignore:
      - "docs/**"
      - "www"
      - "README.md"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: compose-build
        shell: bash
        run: make dc-build
      - name: compose up
        shell: bash
        run: make dc-up
      - name: lint
        shell: bash
        run: docker compose -f ./docker/webserv/docker-compose.yaml exec -T webserv make lint
      - name: make
        shell: bash
        run: docker compose -f ./docker/webserv/docker-compose.yaml exec -T webserv make re
